# AigentForce BD Architecture

- Research Agent → produces company profiles
- Qualification Agent → produces ICP scores
- Pipeline → wires agents together
- Orchestrator → CLI entry point
- Utils → helpers for JSON + logging
- Integrations → CRM, email, LinkedIn stubs
- Tests → schema + pipeline validation
